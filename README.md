# Depression_TTS

unuploaded file:

/logs  -训练好的checkpoint，可以重新训练 不要紧；好像也有其他的训好的（先自己从头跑通-用训好的checkpoint是下策）
/matcha/data/processed_audio_22050
/matcha/data/processed_audio_test_22050 ---需要重新offset处理（导入原数据重新处理即可）
/ckpts --很重要 缺失的话跑不起来 需要传上去
